# Stanley Chen — Personal site

This repository contains the static files for [stanleychenusa.github.io](https://stanleychenusa.github.io/).

The site is intentionally simple and reads like an extended LinkedIn profile. Edit the HTML files directly, commit to `main`, and GitHub Actions will deploy the update automatically.
